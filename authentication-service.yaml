apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.35.0 (HEAD)
  labels:
    io.kompose.service: authentication
  name: authentication
spec:
  ports:
    - name: "8004"
      port: 8004
      targetPort: 3000
  selector:
    io.kompose.service: authentication
