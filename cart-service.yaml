apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.35.0 (HEAD)
  labels:
    io.kompose.service: cart
  name: cart
spec:
  ports:
    - name: "8003"
      port: 8003
      targetPort: 8080
  selector:
    io.kompose.service: cart
